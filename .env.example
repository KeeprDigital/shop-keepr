# `pnpm catalogue:contract` fetches staging's OpenAPI document from
# ${CATALOGUE_BASE_URL}/openapi.json as Consumer #1 (ADR 0013), and
# `pnpm catalogue:seed --from staging` walks staging with the same values.
# The Worker itself reads them as secrets; see .dev.vars.example.
CATALOGUE_BASE_URL=
CATALOGUE_CREDENTIAL=
